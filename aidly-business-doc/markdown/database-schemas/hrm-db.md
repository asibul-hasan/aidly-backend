# HRM DB Script

<!-- Consolidated from the previous aidly-business-doc/backend, frontend, memory, and planning files. -->

## HRM Schema

```sql
-- ============================================================================
-- HRM module â€” PostgreSQL schema  |  run in pgAdmin
-- Generated from JPA entities. EXCLUDES hrm_employee / hrm_designation /
-- hrm_department (already exist). hrm_employee_movement IS included.
-- ============================================================================
BEGIN;

-- ---- tables ----
CREATE TABLE IF NOT EXISTS hrm_attendance (
    att_date date not null,
    early_out_minutes integer,
    in_time time(0),
    is_active smallint not null,
    is_deleted smallint not null,
    is_locked smallint,
    late_minutes integer,
    ot_hours numeric(18,4),
    out_time time(0),
    source smallint,
    status smallint not null,
    worked_hours numeric(18,4),
    attendance_no bigint generated by default as identity,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    leave_application_no bigint,
    row_version bigint not null,
    shift_no bigint,
    updated_at timestamp(6),
    updated_by bigint,
    primary key (attendance_no)
);
CREATE TABLE IF NOT EXISTS hrm_attendance_adjustment (
    att_date date not null,
    is_active smallint not null,
    is_deleted smallint not null,
    requested_in time(0),
    requested_out time(0),
    requested_status smallint not null,
    status smallint not null,
    adjustment_no bigint generated by default as identity,
    approval_request_no bigint,
    approved_at timestamp(6),
    approved_by bigint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    reason TEXT,
    primary key (adjustment_no)
);
CREATE TABLE IF NOT EXISTS hrm_bonus_line (
    base_salary numeric(20,4) not null,
    bonus_amount numeric(20,4) not null,
    is_active smallint not null,
    is_deleted smallint not null,
    bonus_line_no bigint generated by default as identity,
    bonus_run_no bigint not null,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    primary key (bonus_line_no)
);
CREATE TABLE IF NOT EXISTS hrm_bonus_run (
    bonus_type smallint,
    employee_count integer,
    fixed_amount numeric(20,4) not null,
    is_active smallint not null,
    is_deleted smallint not null,
    percentage_of_basic numeric(9,4) not null,
    status smallint not null,
    total_amount numeric(20,4) not null,
    approval_request_no bigint,
    approved_at timestamp(6),
    approved_by bigint,
    bonus_run_no bigint generated by default as identity,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    bonus_id varchar(30) not null,
    bonus_period varchar(60),
    bonus_name varchar(120) not null,
    primary key (bonus_run_no)
);
CREATE TABLE IF NOT EXISTS hrm_candidate (
    application_status smallint not null,
    is_active smallint not null,
    is_deleted smallint not null,
    source smallint,
    branch_no bigint,
    candidate_no bigint generated by default as identity,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    requisition_no bigint,
    resume_file_no bigint,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    mobile_number varchar(20) not null,
    candidate_id varchar(30) not null,
    email varchar(150),
    full_name varchar(150) not null,
    remarks TEXT,
    primary key (candidate_no)
);
CREATE TABLE IF NOT EXISTS hrm_employee_movement (
    effective_date date not null,
    is_active smallint not null,
    is_deleted smallint not null,
    movement_type smallint not null,
    new_salary numeric(20,4),
    status smallint not null,
    approval_request_no bigint,
    approved_at timestamp(6),
    approved_by bigint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    from_branch_no bigint,
    from_department_no bigint,
    from_designation_no bigint,
    from_grade_no bigint,
    movement_no bigint generated by default as identity,
    row_version bigint not null,
    to_branch_no bigint,
    to_department_no bigint,
    to_designation_no bigint,
    to_grade_no bigint,
    updated_at timestamp(6),
    updated_by bigint,
    movement_id varchar(30) not null,
    reason TEXT,
    primary key (movement_no)
);
CREATE TABLE IF NOT EXISTS hrm_final_settlement (
    bonus_payable numeric(20,4),
    gratuity numeric(20,4),
    is_active smallint not null,
    is_deleted smallint not null,
    last_salary numeric(20,4),
    last_working_day date not null,
    leave_encashment numeric(20,4),
    loan_recovery numeric(20,4),
    net_payable numeric(20,4) not null,
    other_deduction numeric(20,4),
    separation_type smallint,
    status smallint not null,
    approval_request_no bigint,
    approved_at timestamp(6),
    approved_by bigint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    row_version bigint not null,
    settlement_no bigint generated by default as identity,
    updated_at timestamp(6),
    updated_by bigint,
    settlement_id varchar(30) not null,
    remarks TEXT,
    primary key (settlement_no)
);
CREATE TABLE IF NOT EXISTS hrm_grade (
    is_active smallint not null,
    is_deleted smallint not null,
    is_overtime_eligible smallint,
    max_salary numeric(20,4),
    min_salary numeric(20,4),
    rank_order integer,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    grade_no bigint generated by default as identity,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    grade_id varchar(20) not null,
    grade_name varchar(100) not null,
    remarks TEXT,
    primary key (grade_no)
);
CREATE TABLE IF NOT EXISTS hrm_holiday (
    holiday_date date not null,
    holiday_type smallint,
    is_active smallint not null,
    is_deleted smallint not null,
    is_recurring smallint,
    leave_year integer,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    holiday_no bigint generated by default as identity,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    holiday_name varchar(150) not null,
    remarks TEXT,
    primary key (holiday_no)
);
CREATE TABLE IF NOT EXISTS hrm_job_requisition (
    budget_max numeric(20,4),
    budget_min numeric(20,4),
    employment_type smallint,
    is_active smallint not null,
    is_deleted smallint not null,
    no_of_vacancies integer not null,
    required_by_date date,
    status smallint not null,
    approval_request_no bigint,
    approved_at timestamp(6),
    approved_by bigint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    department_no bigint not null,
    designation_no bigint not null,
    grade_no bigint,
    requisition_no bigint generated by default as identity,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    requisition_id varchar(30) not null,
    job_description TEXT,
    primary key (requisition_no)
);
CREATE TABLE IF NOT EXISTS hrm_leave_application (
    from_date date not null,
    is_active smallint not null,
    is_deleted smallint not null,
    is_half_day smallint,
    leave_year integer not null,
    status smallint not null,
    to_date date not null,
    total_days numeric(18,4) not null,
    approval_request_no bigint,
    approved_at timestamp(6),
    approved_by bigint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    leave_application_no bigint generated by default as identity,
    leave_type_no bigint not null,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    reason TEXT,
    reliever_employee_no bigint,                -- 2026-07 reliever
    attachment_path varchar(500),               -- 2026-07 medical certificate etc.
    decision_remarks TEXT,                      -- 2026-07-21 approver reason (mandatory on reject)
    is_override smallint not null default 0,    -- 2026-07-21 (doc §7 management override)
    override_reason TEXT,                       -- 2026-07-21
    override_by bigint,                         -- 2026-07-21
    override_at timestamp(6),                   -- 2026-07-21
    is_backdated smallint not null default 0,   -- 2026-07-21 (backdated application report)
    primary key (leave_application_no)
);
CREATE TABLE IF NOT EXISTS hrm_leave_balance (
    employee_no bigint not null,
    leave_type_no bigint not null,              -- 2026-07-21 FIX: entity maps leave_type_no (doc previously said policy_no)
    leave_year integer not null,
    opening_balance numeric(18,4),
    entitled_days numeric(18,4),
    accrued_days numeric(18,4),
    consumed_days numeric(18,4),
    pending_days numeric(18,4),
    encashed_days numeric(18,4),
    lapsed_days numeric(18,4),
    carried_forward numeric(18,4),
    carry_forward_expiry_date date,             -- 2026-07-21 (doc §9 carry-forward expiry)
    leave_balance_no bigint generated by default as identity,
    branch_no bigint,
    is_active smallint not null,                -- 2026-07-21 FIX
    is_deleted smallint not null,               -- 2026-07-21 FIX
    created_at timestamp(6) not null,
    created_by bigint,
    updated_at timestamp(6),                    -- 2026-07-21 FIX
    updated_by bigint,                          -- 2026-07-21 FIX
    deleted_at timestamp(6),
    deleted_by bigint,
    row_version bigint not null,
    primary key (leave_balance_no)
);
-- One balance row per (employee, leave type, year) — without this two concurrent
-- requests can create duplicates and corrupt available-days.
CREATE UNIQUE INDEX IF NOT EXISTS ux_leave_balance_emp_type_year
    ON hrm_leave_balance (employee_no, leave_type_no, leave_year) WHERE is_deleted = 0;
CREATE TABLE IF NOT EXISTS hrm_leave_ledger (
    balance_after numeric(18,4),
    days numeric(18,4) not null,
    is_active smallint not null,
    is_deleted smallint not null,
    leave_year integer not null,
    movement_date date not null,
    movement_type smallint not null,
    ref_doc_type smallint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    leave_ledger_no bigint generated by default as identity,
    leave_type_no bigint not null,
    ref_doc_no bigint,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    primary key (leave_ledger_no)
);
CREATE TABLE IF NOT EXISTS hrm_leave_policy_setup (
    policy_name varchar(100) not null,
    leave_type_no bigint not null,
    employee_group_no bigint,
    accrual_method smallint,
    default_days numeric(18,4),
    work_days_per_leave_day integer,
    proration_method smallint,
    eligibility_service_months integer,
    is_carry_forward_allowed smallint,
    max_carry_forward_days numeric(18,4),
    carry_forward_validity_months integer,
    is_encashable smallint,
    max_encashable_days numeric(18,4),
    encashment_formula varchar(100),
    is_lapsable smallint,
    max_dept_leave_percentage numeric(18,4),
    min_balance_for_encashment numeric(18,4),   -- 2026-07-21 (doc §9 encashment eligibility)
    department_no bigint,                       -- 2026-07-21 (doc §4 dept-wise policy)
    shift_no bigint,                            -- 2026-07-21 (doc §4 shift restriction)
    specialized_config TEXT,
    policy_no bigint generated by default as identity,
    branch_no bigint,
    is_active smallint not null,                -- 2026-07-21 FIX: entity extends AuditEntity
    is_deleted smallint not null,               -- 2026-07-21 FIX: repos call findBy...AndIsDeleted
    created_at timestamp(6) not null,
    created_by bigint,
    updated_at timestamp(6),                    -- 2026-07-21 FIX
    updated_by bigint,                          -- 2026-07-21 FIX
    deleted_at timestamp(6),
    deleted_by bigint,
    row_version bigint not null,
    primary key (policy_no)
);
CREATE TABLE IF NOT EXISTS hrm_leave_application_rule (
    policy_no bigint not null,
    min_duration_per_app numeric(18,4),
    max_duration_per_app numeric(18,4),
    notice_period_days integer,
    max_retroactive_days integer,
    exclude_weekends smallint,
    exclude_holidays smallint,
    allow_negative_balance smallint,
    max_overdraft_days numeric(18,4),
    is_shift_critical smallint,
    is_approval_required smallint,
    is_reliever_mandatory smallint,
    attachment_required_after_days numeric(18,4),
    sla_timeout_hours integer,
    escalation_role_no bigint,
    allow_override smallint not null default 0, -- 2026-07-21 (doc §7 override)
    rule_no bigint generated by default as identity,
    branch_no bigint,
    is_active smallint not null,                -- 2026-07-21 FIX
    is_deleted smallint not null,               -- 2026-07-21 FIX
    created_at timestamp(6) not null,
    created_by bigint,
    updated_at timestamp(6),                    -- 2026-07-21 FIX
    updated_by bigint,                          -- 2026-07-21 FIX
    deleted_at timestamp(6),
    deleted_by bigint,
    row_version bigint not null,
    primary key (rule_no)
);

-- ── Leave encashment (doc §9, UC 7.2) ──────────────────────────────────────
-- status: 1=Draft 2=Applied 3=Approved 4=Rejected 5=Paid (enforced in middleware)
CREATE TABLE IF NOT EXISTS hrm_leave_encashment (
    encashment_no bigint generated by default as identity,
    employee_no bigint not null,
    leave_type_no bigint not null,
    leave_year integer not null,
    requested_days numeric(18,4) not null,
    approved_days numeric(18,4),
    rate_per_day numeric(18,4),
    amount numeric(18,4),
    status smallint not null default 1,
    approval_request_no bigint,
    payroll_ref_no bigint,
    remarks TEXT,
    branch_no bigint,
    is_active smallint not null default 1,
    is_deleted smallint not null default 0,
    created_at timestamp(6) not null default CURRENT_TIMESTAMP,
    created_by bigint,
    updated_at timestamp(6),
    updated_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    row_version bigint not null default 1,
    primary key (encashment_no)
);

-- ── Leave audit trail (doc §11) ────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS hrm_leave_audit_log (
    audit_no bigint generated by default as identity,
    document_type varchar(30) not null,          -- HRM_1301 / HRM_1321 …
    document_no bigint not null,
    employee_no bigint,
    action varchar(40) not null,                 -- APPLY/APPROVE/REJECT/CANCEL/OVERRIDE/MODIFY
    old_status smallint,
    new_status smallint,
    old_value TEXT,
    new_value TEXT,
    remarks TEXT,
    acted_by bigint,
    acted_at timestamp(6) not null default CURRENT_TIMESTAMP,
    ip_address varchar(45),
    company_no bigint,
    branch_no bigint,
    primary key (audit_no)
);

-- ── Leave notifications (doc §10, UC 9.1/9.2) ──────────────────────────────
-- send_status: 1=Pending 2=Sent 3=Failed (enforced in middleware)
CREATE TABLE IF NOT EXISTS hrm_leave_notification (
    notification_no bigint generated by default as identity,
    recipient_user_no bigint not null,
    document_type varchar(30),
    document_no bigint,
    event_type varchar(40) not null,             -- APPLIED/APPROVED/REJECTED/BALANCE_LOW/…
    channel varchar(20) not null default 'PORTAL',
    subject varchar(200),
    body TEXT,
    send_status smallint not null default 1,
    sent_at timestamp(6),
    is_read smallint not null default 0,
    read_at timestamp(6),
    company_no bigint,
    branch_no bigint,
    created_at timestamp(6) not null default CURRENT_TIMESTAMP,
    primary key (notification_no)
);
CREATE TABLE IF NOT EXISTS hrm_leave_type (
    leave_type_id varchar(20) not null,
    leave_type_name varchar(100) not null,
    is_paid smallint,
    statutory_category varchar(20) default 'NONE', -- 2026-07 replaced is_statutory
    accrual_method smallint,                       -- 2026-07-21
    requires_approval smallint not null default 1, -- 2026-07-21
    default_days_per_year numeric(18,4),           -- 2026-07-21
    -- doc §3 "Define attributes"
    is_carry_forward_allowed smallint not null default 0,  -- 2026-07-21 (was is_carry_forward)
    max_carry_forward_days numeric(18,4),
    carry_forward_validity_months integer,
    is_encashable smallint not null default 0,
    is_lapsable smallint not null default 0,
    documentation_required smallint not null default 0,
    min_duration_days numeric(18,4),
    max_duration_days numeric(18,4),
    eligibility_service_months integer default 0,
    is_active smallint,
    is_deleted smallint not null,                  -- 2026-07-21 FIX (AuditEntity)
    remarks TEXT,
    leave_type_no bigint generated by default as identity,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    updated_at timestamp(6),                       -- 2026-07-21 FIX
    updated_by bigint,                             -- 2026-07-21 FIX
    deleted_at timestamp(6),
    deleted_by bigint,
    row_version bigint not null,
    primary key (leave_type_no)
);
CREATE UNIQUE INDEX IF NOT EXISTS ux_leave_type_id
    ON hrm_leave_type (leave_type_id) WHERE is_deleted = 0;
CREATE TABLE IF NOT EXISTS hrm_loan_advance (
    disbursement_date date,
    installment_amount numeric(20,4) not null,
    installment_count integer not null,
    is_active smallint not null,
    is_deleted smallint not null,
    loan_type smallint,
    outstanding_amount numeric(20,4) not null,
    principal_amount numeric(20,4) not null,
    recovered_amount numeric(20,4) not null,
    recovery_start date,
    status smallint not null,
    approval_request_no bigint,
    approved_at timestamp(6),
    approved_by bigint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    loan_no bigint generated by default as identity,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    loan_id varchar(30) not null,
    reason TEXT,
    primary key (loan_no)
);
CREATE TABLE IF NOT EXISTS hrm_offer (
    is_active smallint not null,
    is_deleted smallint not null,
    joining_date date not null,
    offered_salary numeric(20,4) not null,
    status smallint not null,
    branch_no bigint,
    candidate_no bigint not null,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    designation_no bigint,
    grade_no bigint,
    offer_file_no bigint,
    offer_no bigint generated by default as identity,
    onboarded_employee_no bigint,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    offer_id varchar(30) not null,
    notes TEXT,
    primary key (offer_no)
);
CREATE TABLE IF NOT EXISTS hrm_overtime (
    hourly_rate numeric(20,4) not null,
    hours numeric(9,2) not null,
    is_active smallint not null,
    is_deleted smallint not null,
    ot_amount numeric(20,4) not null,
    ot_date date not null,
    rate_multiplier numeric(9,2) not null,
    status smallint not null,
    approval_request_no bigint,
    approved_at timestamp(6),
    approved_by bigint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    overtime_no bigint generated by default as identity,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    overtime_id varchar(30) not null,
    reason TEXT,
    primary key (overtime_no)
);
CREATE TABLE IF NOT EXISTS hrm_payroll_run (
    employee_count integer not null,
    is_active smallint not null,
    is_deleted smallint not null,
    period_end date not null,
    period_start date not null,
    run_type smallint not null,
    status smallint not null,
    total_deduction numeric(20,4) not null,
    total_gross numeric(20,4) not null,
    total_net numeric(20,4) not null,
    pay_period varchar(7) not null,
    approval_request_no bigint,
    approved_at timestamp(6),
    approved_by bigint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    payroll_run_no bigint generated by default as identity,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    primary key (payroll_run_no)
);
CREATE TABLE IF NOT EXISTS hrm_payslip (
    absent_days numeric(18,4),
    gross_earning numeric(20,4) not null,
    is_active smallint not null,
    is_deleted smallint not null,
    leave_days numeric(18,4),
    lwp_days numeric(18,4),
    net_pay numeric(20,4) not null,
    pay_method smallint,
    payment_status smallint not null,
    present_days numeric(18,4),
    total_deduction numeric(20,4) not null,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    payroll_run_no bigint not null,
    payslip_no bigint generated by default as identity,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    primary key (payslip_no)
);
CREATE TABLE IF NOT EXISTS hrm_payslip_dtl (
    amount numeric(20,4) not null,
    component_type smallint not null,
    is_active smallint not null,
    is_deleted smallint not null,
    branch_no bigint,
    component_no bigint not null,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    payslip_dtl_no bigint generated by default as identity,
    payslip_no bigint not null,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    primary key (payslip_dtl_no)
);
CREATE TABLE IF NOT EXISTS hrm_salary_component (
    affects_net smallint,
    calc_type smallint not null,
    calc_value numeric(20,4),
    component_type smallint not null,
    country_code varchar(2),
    display_order integer,
    is_active smallint not null,
    is_deleted smallint not null,
    is_statutory smallint,
    is_taxable smallint,
    branch_no bigint,
    component_no bigint generated by default as identity,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    component_id varchar(20) not null,
    gl_account_code varchar(30),
    component_name varchar(100) not null,
    remarks TEXT,
    primary key (component_no)
);
CREATE TABLE IF NOT EXISTS hrm_salary_structure (
    effective_from date not null,
    effective_to date,
    gross_salary numeric(20,4) not null,
    is_active smallint not null,
    is_deleted smallint not null,
    status smallint not null,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    row_version bigint not null,
    salary_structure_no bigint generated by default as identity,
    updated_at timestamp(6),
    updated_by bigint,
    remarks TEXT,
    primary key (salary_structure_no)
);
CREATE TABLE IF NOT EXISTS hrm_salary_structure_dtl (
    amount numeric(20,4) not null,
    is_active smallint not null,
    is_deleted smallint not null,
    branch_no bigint,
    component_no bigint not null,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    row_version bigint not null,
    salary_structure_no bigint not null,
    structure_dtl_no bigint generated by default as identity,
    updated_at timestamp(6),
    updated_by bigint,
    primary key (structure_dtl_no)
);
CREATE TABLE IF NOT EXISTS hrm_shift (
    break_minutes integer,
    end_time time(0) not null,
    grace_minutes integer,
    half_day_minutes integer,
    is_active smallint not null,
    is_deleted smallint not null,
    is_night_shift smallint,
    start_time time(0) not null,
    weekly_off_mask smallint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    row_version bigint not null,
    shift_no bigint generated by default as identity,
    updated_at timestamp(6),
    updated_by bigint,
    shift_id varchar(20) not null,
    shift_name varchar(100) not null,
    remarks TEXT,
    primary key (shift_no)
);
CREATE TABLE IF NOT EXISTS hrm_shift_roster (
    from_date date not null,
    is_active smallint not null,
    is_deleted smallint not null,
    status smallint not null,
    to_date date not null,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    roster_no bigint generated by default as identity,
    row_version bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    roster_id varchar(30) not null,
    roster_name varchar(120) not null,
    primary key (roster_no)
);
CREATE TABLE IF NOT EXISTS hrm_shift_roster_line (
    is_active smallint not null,
    is_deleted smallint not null,
    weekly_off smallint,
    branch_no bigint,
    created_at timestamp(6) not null,
    created_by bigint,
    deleted_at timestamp(6),
    deleted_by bigint,
    employee_no bigint not null,
    roster_line_no bigint generated by default as identity,
    roster_no bigint not null,
    row_version bigint not null,
    shift_no bigint not null,
    updated_at timestamp(6),
    updated_by bigint,
    primary key (roster_line_no)
);

-- ---- indexes ----
CREATE INDEX IF NOT EXISTS idx_att_branch_date_deleted on hrm_attendance (branch_no, att_date, is_deleted);
CREATE INDEX IF NOT EXISTS idx_att_emp_date_deleted on hrm_attendance (employee_no, att_date, is_deleted);
CREATE INDEX IF NOT EXISTS idx_attadj_emp_status_deleted on hrm_attendance_adjustment (employee_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_attadj_branch_deleted on hrm_attendance_adjustment (branch_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_bonus_line_run_deleted on hrm_bonus_line (bonus_run_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_bonus_branch_status_deleted on hrm_bonus_run (branch_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_candidate_branch_status_deleted on hrm_candidate (branch_no, application_status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_candidate_req_deleted on hrm_candidate (requisition_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_movement_emp_deleted on hrm_employee_movement (employee_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_movement_branch_status_deleted on hrm_employee_movement (branch_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_settlement_emp_deleted on hrm_final_settlement (employee_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_settlement_branch_status_deleted on hrm_final_settlement (branch_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_grade_branch_deleted on hrm_grade (branch_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_holiday_branch_date_deleted on hrm_holiday (branch_no, holiday_date, is_deleted);
CREATE INDEX IF NOT EXISTS idx_req_branch_status_deleted on hrm_job_requisition (branch_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_req_dept_deleted on hrm_job_requisition (department_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_leaveapp_emp_status_deleted on hrm_leave_application (employee_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_leaveapp_branch_deleted on hrm_leave_application (branch_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_leavebal_emp_type_year on hrm_leave_balance (employee_no, leave_type_no, leave_year, is_deleted);
CREATE INDEX IF NOT EXISTS idx_leaveledger_emp_type_year on hrm_leave_ledger (employee_no, leave_type_no, leave_year);
CREATE INDEX IF NOT EXISTS idx_leaveledger_ref on hrm_leave_ledger (ref_doc_type, ref_doc_no);
CREATE INDEX IF NOT EXISTS idx_leavetype_branch_deleted on hrm_leave_type (branch_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_loan_emp_deleted on hrm_loan_advance (employee_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_loan_branch_status_deleted on hrm_loan_advance (branch_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_offer_candidate_deleted on hrm_offer (candidate_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_offer_branch_status_deleted on hrm_offer (branch_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_ot_emp_deleted on hrm_overtime (employee_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_ot_branch_status_deleted on hrm_overtime (branch_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_payrun_branch_period_deleted on hrm_payroll_run (branch_no, pay_period, is_deleted);
CREATE INDEX IF NOT EXISTS idx_payslip_run_deleted on hrm_payslip (payroll_run_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_payslip_emp_deleted on hrm_payslip (employee_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_payslipdtl_slip_deleted on hrm_payslip_dtl (payslip_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_salcomp_branch_deleted on hrm_salary_component (branch_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_salstruct_emp_deleted on hrm_salary_structure (employee_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_salstruct_branch_deleted on hrm_salary_structure (branch_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_salstructdtl_header_deleted on hrm_salary_structure_dtl (salary_structure_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_shift_branch_deleted on hrm_shift (branch_no, is_deleted);
CREATE INDEX IF NOT EXISTS idx_roster_branch_status_deleted on hrm_shift_roster (branch_no, status, is_deleted);
CREATE INDEX IF NOT EXISTS idx_roster_line_roster_deleted on hrm_shift_roster_line (roster_no, is_deleted);

COMMIT;

```

## HRM Menu Seed

```sql
-- ============================================================================
-- HRM + INV menu seed + enrollment for COMPANY 2  (PostgreSQL / pgAdmin)
-- Idempotent: re-runnable; parents referenced by code (no hardcoded IDs).
-- form_id drives RBAC (frontend sends X-Form-Id); route_path = frontend nav route.
-- created_by is stamped with an existing user (min live sys_user.user_no).
-- ============================================================================
BEGIN;

-- 1) Modules ----------------------------------------------------------------
INSERT INTO sys_module (module_code, module_name, module_route, module_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT 'HRM','Human Resource Management','hrm','groups',20,1,0,1,now(),(SELECT min(user_no) FROM sys_user WHERE is_deleted = 0)
WHERE NOT EXISTS (SELECT 1 FROM sys_module WHERE module_code='HRM');
INSERT INTO sys_module (module_code, module_name, module_route, module_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT 'INV','Inventory','inv','inventory_2',30,1,0,1,now(),(SELECT min(user_no) FROM sys_user WHERE is_deleted = 0)
WHERE NOT EXISTS (SELECT 1 FROM sys_module WHERE module_code='INV');

-- 2) Submodules -------------------------------------------------------------
INSERT INTO sys_submodule (module_no, submodule_code, submodule_name, submodule_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT m.module_no, 'HRM-SETUP', 'HR Setup', 'settings', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_module m WHERE m.module_code='HRM'
  AND NOT EXISTS (SELECT 1 FROM sys_submodule s WHERE s.module_no=m.module_no AND s.submodule_code='HRM-SETUP');
INSERT INTO sys_submodule (module_no, submodule_code, submodule_name, submodule_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT m.module_no, 'HRM-ATT', 'Attendance & Time', 'schedule', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_module m WHERE m.module_code='HRM'
  AND NOT EXISTS (SELECT 1 FROM sys_submodule s WHERE s.module_no=m.module_no AND s.submodule_code='HRM-ATT');
INSERT INTO sys_submodule (module_no, submodule_code, submodule_name, submodule_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT m.module_no, 'HRM-PAY', 'Payroll', 'payments', 3, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_module m WHERE m.module_code='HRM'
  AND NOT EXISTS (SELECT 1 FROM sys_submodule s WHERE s.module_no=m.module_no AND s.submodule_code='HRM-PAY');
INSERT INTO sys_submodule (module_no, submodule_code, submodule_name, submodule_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT m.module_no, 'HRM-LEAVE', 'Leave Management', 'event_available', 4, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_module m WHERE m.module_code='HRM'
  AND NOT EXISTS (SELECT 1 FROM sys_submodule s WHERE s.module_no=m.module_no AND s.submodule_code='HRM-LEAVE');
INSERT INTO sys_submodule (module_no, submodule_code, submodule_name, submodule_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT m.module_no, 'HRM-REC', 'Recruitment', 'person_search', 5, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_module m WHERE m.module_code='HRM'
  AND NOT EXISTS (SELECT 1 FROM sys_submodule s WHERE s.module_no=m.module_no AND s.submodule_code='HRM-REC');
INSERT INTO sys_submodule (module_no, submodule_code, submodule_name, submodule_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT m.module_no, 'INV-PROD', 'Product & Setup', 'inventory_2', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_module m WHERE m.module_code='INV'
  AND NOT EXISTS (SELECT 1 FROM sys_submodule s WHERE s.module_no=m.module_no AND s.submodule_code='INV-PROD');
INSERT INTO sys_submodule (module_no, submodule_code, submodule_name, submodule_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT m.module_no, 'INV-STOCK', 'Stock Operations', 'warehouse', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_module m WHERE m.module_code='INV'
  AND NOT EXISTS (SELECT 1 FROM sys_submodule s WHERE s.module_no=m.module_no AND s.submodule_code='INV-STOCK');
INSERT INTO sys_submodule (module_no, submodule_code, submodule_name, submodule_icon, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT m.module_no, 'INV-WH', 'Warehouse & Transfer', 'move_to_inbox', 3, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_module m WHERE m.module_code='INV'
  AND NOT EXISTS (SELECT 1 FROM sys_submodule s WHERE s.module_no=m.module_no AND s.submodule_code='INV-WH');

-- 3) Menus (forms) ----------------------------------------------------------
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1001', 'Employee Management', 'form', '/hrm/form/employee-management', 'badge', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-SETUP'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1001');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1002', 'Department & Designation Setup', 'form', '/hrm/form/department-designation-setup', 'account_tree', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-SETUP'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1002');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1003', 'Shift Setup', 'form', '/hrm/form/shift-setup', 'schedule', 3, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-SETUP'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1003');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1004', 'Grade / Pay-Scale Setup', 'form', '/hrm/form/grade-setup', 'grade', 4, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-SETUP'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1004');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1005', 'Leave Type Setup', 'form', '/hrm/form/leave-type-setup', 'event_note', 5, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-SETUP'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1005');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1006', 'Holiday Calendar', 'form', '/hrm/form/holiday-calendar', 'calendar_month', 6, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-SETUP'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1006');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1007', 'Salary Component Setup', 'form', '/hrm/form/salary-component-setup', 'tune', 7, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-SETUP'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1007');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1008', 'HR Policy / Settings', 'form', '/hrm/form/hr-settings', 'policy', 8, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-SETUP'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1008');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1009', 'Tax Slab Setup', 'form', '/hrm/form/tax-slab-setup', 'request_quote', 9, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-SETUP'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1009');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1101', 'Manual Attendance', 'form', '/hrm/form/manual-attendance', 'how_to_reg', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-ATT'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1101');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1102', 'Device-Sync Attendance', 'form', '/hrm/form/device-sync-attendance', 'fingerprint', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-ATT'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1102');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1103', 'Attendance Adjustment', 'form', '/hrm/form/attendance-adjustment', 'edit_calendar', 3, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-ATT'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1103');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1104', 'Shift Roster', 'form', '/hrm/form/shift-roster', 'calendar_month', 4, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-ATT'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1104');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1105', 'Overtime', 'form', '/hrm/form/overtime', 'more_time', 5, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-ATT'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1105');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1106', 'Promotion / Transfer', 'form', '/hrm/form/promotion-transfer', 'swap_horiz', 6, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-ATT'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1106');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1201', 'Salary Structure Setup', 'form', '/hrm/form/salary-structure-setup', 'account_balance_wallet', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-PAY'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1201');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1202', 'Salary Processing', 'form', '/hrm/form/salary-processing', 'payments', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-PAY'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1202');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1203', 'Salary Sheet', 'form', '/hrm/form/salary-sheet', 'receipt_long', 3, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-PAY'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1203');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1204', 'Payslip Generator', 'form', '/hrm/form/payslip-generator', 'receipt', 4, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-PAY'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1204');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1205', 'Bonus / Festival', 'form', '/hrm/form/bonus-festival', 'celebration', 5, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-PAY'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1205');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1206', 'Loan / Advance', 'form', '/hrm/form/loan-advance', 'payments', 6, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-PAY'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1206');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1207', 'Final Settlement', 'form', '/hrm/form/final-settlement', 'price_check', 7, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-PAY'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1207');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1301', 'Leave Application', 'form', '/hrm/form/leave-application', 'event_available', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-LEAVE'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1301');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1303', 'Leave Balance', 'form', '/hrm/form/leave-balance', 'event_available', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-LEAVE'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1303');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1401', 'Job Requisition', 'form', '/hrm/form/job-requisition', 'work_outline', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-REC'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1401');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1402', 'Candidate / Application', 'form', '/hrm/form/candidate', 'person_search', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-REC'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1402');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'HRM_1404', 'Offer & Onboarding', 'form', '/hrm/form/offer-onboarding', 'description', 3, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='HRM-REC'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='HRM_1404');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_1001', 'Product Master', 'form', '/inv/form/product-master-setup', 'inventory_2', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-PROD'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_1001');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_1002', 'Barcode Generator', 'form', '/inv/form/barcode-generator', 'qr_code', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-PROD'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_1002');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_1003', 'Category Setup', 'form', '/inv/form/category-setup', 'category', 3, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-PROD'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_1003');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_1004', 'Brand Setup', 'form', '/inv/form/brand-setup', 'sell', 4, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-PROD'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_1004');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_1005', 'Unit of Measure Setup', 'form', '/inv/form/unit-of-measure-setup', 'straighten', 5, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-PROD'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_1005');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_1006', 'Product Attribute Setup', 'form', '/inv/form/product-attribute-setup', 'tune', 6, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-PROD'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_1006');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_1101', 'Opening Stock', 'form', '/inv/form/opening-stock-entry', 'inventory', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-STOCK'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_1101');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_1102', 'Stock Adjustment', 'form', '/inv/form/stock-adjustment', 'edit_note', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-STOCK'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_1102');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_2001', 'Warehouse Setup', 'form', '/inv/form/warehouse-setup', 'warehouse', 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-WH'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_2001');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_2002', 'Rack/Shelf Setup', 'form', '/inv/form/rack-shelf-setup', 'shelves', 2, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-WH'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_2002');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_2003', 'Stock Transfer', 'form', '/inv/form/stock-transfer', 'local_shipping', 3, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-WH'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_2003');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_2004', 'Batch / Expiry Management', 'form', '/inv/form/batch-expiry-management', 'event_busy', 4, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-WH'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_2004');
INSERT INTO sys_menu (submodule_no, form_id, form_name, menu_type, route_path, icon_name, order_sl, is_active, is_deleted, row_version, created_at, created_by)
SELECT s.submodule_no, 'INV_2005', 'Reorder Level Setup', 'form', '/inv/form/reorder-level-setup', 'production_quantity_limits', 5, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0) FROM sys_submodule s WHERE s.submodule_code='INV-WH'
  AND NOT EXISTS (SELECT 1 FROM sys_menu mn WHERE mn.form_id='INV_2005');

-- 4) Enroll ALL HRM + INV menus for COMPANY 2 (branch_no NULL = all branches) --
INSERT INTO sys_enroll_menu (company_no, branch_no, menu_no, is_lifetime, is_active, is_deleted, row_version, created_at, created_by)
SELECT 2, NULL, mn.menu_no, 1, 1, 0, 1, now(), (SELECT min(user_no) FROM sys_user WHERE is_deleted = 0)
FROM sys_menu mn
JOIN sys_submodule s  ON s.submodule_no = mn.submodule_no
JOIN sys_module    mo ON mo.module_no   = s.module_no
WHERE mo.module_code IN ('HRM','INV')
  AND NOT EXISTS (SELECT 1 FROM sys_enroll_menu e WHERE e.company_no=2 AND e.menu_no=mn.menu_no AND e.is_deleted=0);

COMMIT;

```

## HRM Employee User Provisioning

```sql
-- ============================================================================
-- HRM employee â†’ user provisioning: add the is_create_user flag (PostgreSQL).
-- hrm_employee already exists, so this is an additive column. Safe to re-run.
-- ============================================================================
ALTER TABLE hrm_employee
    ADD COLUMN IF NOT EXISTS is_create_user smallint NOT NULL DEFAULT 0;

-- Backfill: mark employees who already have a (non-deleted) login user.
UPDATE hrm_employee e
   SET is_create_user = 1
 WHERE e.is_create_user <> 1
   AND EXISTS (SELECT 1 FROM sys_user u
                WHERE u.employee_no = e.employee_no
                  AND u.is_deleted = 0);

```

---

## HRM Applied Migration Log

Every migration applied to the database is recorded here, newest first. The
schema blocks above are kept in sync with these, so this file always reflects
the CURRENT state of the HRM tables.

| Applied | Migration file | Scope |
|---|---|---|
| 2026-07-21 | `2026-07_hrm_leave_management_alignment_pure_ddl.sql` | Leave module alignment with the HRM Leave business document |

### 2026-07-21 — Leave management alignment

Applied via `db-migration/2026-07_hrm_leave_management_alignment_pure_ddl.sql`.

**Critical fix — missing audit columns.** `hrm_leave_policy_setup` (and
defensively every other leave table) lacked `is_active` / `is_deleted` /
`updated_at` / `updated_by` / `row_version`, while the JPA entities extend
`AuditEntity` and the repositories call `findBy…AndIsDeleted(…)`. Those queries
failed at runtime, which is why the configured leave policy was never applied.

**Renamed (data preserved, not dropped):**

| Table | Old | New |
|---|---|---|
| `hrm_leave_type` | `is_carry_forward` | `is_carry_forward_allowed` |
| `hrm_leave_policy_setup` | `is_carry_forward` | `is_carry_forward_allowed` |
| `hrm_leave_type` | `is_statutory` | `legacy_is_statutory` (superseded by `statutory_category`) |

**Columns added:** leave-type attributes (lapsable, documentation_required,
min/max duration, carry-forward validity, eligibility months); policy
department/shift scope + `min_balance_for_encashment`; application rule
`allow_override`; application `decision_remarks`, override_* and `is_backdated`;
balance `carry_forward_expiry_date`.

**Tables added:** `hrm_leave_encashment`, `hrm_leave_audit_log`,
`hrm_leave_notification`. (SLA/escalation columns on `sys_approval_step` /
`sys_approval_request` and the new `sys_approval_delegation` table are recorded
in `sys-db.md`.)

**Seeded:** the 10 leave types named in the business document (Annual, Casual,
Sick, Maternity, Paternity, Bereavement, Compensatory Off, Unpaid, Study,
Sabbatical) — insert-if-absent, never overwrites an existing row.
